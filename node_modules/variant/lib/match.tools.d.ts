/**
 * Create a function that returns a value after being called.
 * @param x the value to be returned
 */
export declare function constant<T>(x: T): () => T;
export declare const just: typeof constant;
/**
 * Extract the payload element from the object and return it.
 * @example
 * ```
 * match(object, {
 *     ...
 *     case: unpack,
 *     ...
 * })
 * ```
 */
export declare const unpack: <T>(x: {
    payload: T;
}) => T;
//# sourceMappingURL=match.tools.d.ts.map